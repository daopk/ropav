/// <reference types="vitest/globals" />
// Augments `vitest/browser` with the provider-specific shapes - notably `CDPSession`,
// which is an empty interface until a provider fills it in.
/// <reference types="@vitest/browser-playwright" />
/// <reference types="@testing-library/jest-dom" />
