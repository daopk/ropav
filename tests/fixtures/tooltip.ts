import type { TooltipPlacement } from '../../src/components/tooltip/types';

export const placements: TooltipPlacement[] = ['top', 'right', 'bottom', 'left'];

export const colors = [
    'primary',
    'secondary',
    'success',
    'warning',
    'danger',
    'info',
    'neutral',
] as const;
