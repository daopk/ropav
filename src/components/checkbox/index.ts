export { default as Checkbox } from './checkbox.vue';
export { checkboxParts } from './types';
export type {
    CheckboxColor,
    CheckboxPart,
    CheckboxProps,
    CheckboxRadius,
    CheckboxSize,
    CheckboxVariant,
} from './types';
