# Agent Notes

- Always use Conventional Commits for commit messages.
- Always keep production components and published bundles zero-VDOM. Use Vue Vapor and never introduce VDOM APIs such as `h`, `VNode`, `defineComponent`, or `createVNode` into production component code.
